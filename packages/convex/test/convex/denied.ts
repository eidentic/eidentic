/** Test fixture proving the secure factory default denies public invocations at runtime. */
import { eidenticFunctions } from "../../src/server.js";

export const { getBlocks } = eidenticFunctions();
