---
"eidentic": minor
"@eidentic/tools": patch
---

Add the `eidentic/testing` subpath for no-key fresh-install smoke tests and adapter conformance helpers.

Clean up the tools glob helper re-export so release builds stay quieter.
